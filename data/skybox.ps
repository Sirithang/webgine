precision mediump float;

varying vec4 oWorldPos;
varying vec3 oNormal;
varying vec4 oTexcoord;

uniform samplerCube sSkybox; 
uniform vec4 uData;

void main()                                  
{
	/*float slope = dot(oNormal.xyz, vec3(0,1,0));

	slope = (slope + 1.0) * 0.5;
	slope = clamp(slope+0.2, 0.0, 1.0);

	vec3 ground = vec3(200.0/255.0, 139.0/255.0, 48.0/255.0);
	vec3 sky = vec3(67.0/255.0, 139.0/255.0, 102.0/255.0);

	vec3 res = mix(ground, sky, slope);*/

	gl_FragColor = textureCube( sSkybox, oNormal.xyz );
}